{
	"type": "module",
	"name": "@oh-my-soup/browser-relay",
	"version": "0.1.0",
	"private": true,
	"description": "Chrome extension that lets the oms browser tool drive your existing tabs (relay server lives in the oms CLI: `oms browser-relay`)",
	"homepage": "https://github.com/pickpocket/oh-my-soup",
	"author": "Can Boluk",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/pickpocket/oh-my-soup.git",
		"directory": "packages/browser-relay"
	},
	"scripts": {
		"build": "bun scripts/build-extension.ts",
		"check": "biome check . && tsgo -p tsconfig.json --noEmit",
		"lint": "biome lint .",
		"fix": "biome check --write --unsafe .",
		"fmt": "biome format --write ."
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"puppeteer-core": "catalog:"
	},
	"engines": {
		"bun": ">=1.3.14"
	}
}
