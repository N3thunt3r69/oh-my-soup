Publish the canonical result of the current OMS-native security scan. Call this exactly once after every in-scope file and candidate has a final disposition. Supply only evidence grounded in repository files inspected during this scan. This tool validates, fingerprints, assigns OMS-owned IDs, writes the canonical security store, and creates SARIF. Do not invent IDs or edit the store directly.
